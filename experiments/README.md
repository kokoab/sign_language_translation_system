# Experiments

Ad-hoc experiments and one-off scripts should move here once their imports are untangled.

The current `scripts/` directory is still left in place because several utilities directly reference `src/`, `src_v16/`, local datasets, and mobile export paths.
