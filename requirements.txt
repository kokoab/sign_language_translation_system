# SLT — Sign Language Translator (full pipeline)
# Install: pip install -r requirements.txt
#
# VENV SETUP (Windows / macOS / Linux):
#   python -m venv venv
#   venv\Scripts\activate        (Windows)
#   source venv/bin/activate     (macOS/Linux)
#   pip install -r requirements.txt
#
# If you get "mediapipe has no attribute 'solutions'":
#   - Ensure 64-bit Python (not 32-bit). Check: python -c "import struct; print(struct.calcsize('P')*8)"
#   - Do NOT have a folder/file named "mediapipe" in your project (shadows the package)
#   - pip uninstall mediapipe && pip install mediapipe==0.10.10 --no-cache-dir

# Core (extraction, inference)
opencv-python>=4.8
mediapipe>=0.10.9
numpy>=1.24

# ML / training & inference
torch>=2.0
transformers>=4.30
datasets>=2.14

# Data & Stage 3 generation
pandas>=1.5
inflect>=6.0

# Evaluation & viz
scikit-learn>=1.2
matplotlib>=3.7
seaborn>=0.12
