# SLT — Sign Language Translator (full pipeline)
# Install: pip install -r requirements.txt
# Run scripts with the same Python that pip uses (e.g. conda: conda activate base;
#   or: /path/to/python test/SLT_test.py).

# Core (existing)
opencv-python>=4.8
mediapipe>=0.10.9
numpy>=1.24

# ML / training & inference
torch>=2.0
transformers>=4.30
datasets>=2.14

# Data & Stage 3 generation
pandas>=1.5
inflect>=6.0

# Evaluation & viz
scikit-learn>=1.2
matplotlib>=3.7
seaborn>=0.12
